// @flow
type HabitScore = {
    habitName: String,
    username: String,
    currentStreak: Number,
    bestStreak: Number,
    totalScore: Number,
    startDate: Date,
    lastComplted: Date
};

export type { HabitScore };
